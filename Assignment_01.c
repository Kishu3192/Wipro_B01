/*

// 
Linear search algorithem

step 1 start

step 2 set a[10]
       set i = 0

step 3 input searching value

step 4 while (i<10) go to step 5 & 6

step 5 if a[i] = value then
            print value is found and go to step 8

step 6 i = i + 1

step 7 if i > 10 then 
         print value is not found

step 8  End         
       


// Binary search algorithem


step 1 start
       
       arr[0],arr[n]
       find mid using(arr[0] + arr[n])/2
       
step 2    while arr[0] <= arr[n] and a[mid] != value
          go to step 3 & 4
    
step 3  if value < a[mid] then
        set arr[n] = mid - 1
        else
        set arr[0] mid + 1

step 4 set mid = (arr[0] + arr[n])/2 go to step 2

step 5 if a[mid] = value then
       set location = mid 
       else
       set location = null

step 6 stop       
*/